# Engine fixtures

Implementation-neutral reader, typechecker, and canonical-artifact fixtures.
The OCaml gate consumes these directly; equivalent behavior is covered by the
TypeScript suites under `packages/ts/test`.
