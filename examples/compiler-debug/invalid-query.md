# Invalid Query Fixture

This file is intentionally not referenced by `README.md`. Use it for manual
diagnostic-path checks without breaking `pnpm devtools compiler examples`.

```lisp
(define-query invalid-missing-source
  (:select [employee/name]))
```
